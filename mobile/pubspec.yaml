name: photosync
description: Photo sync backup app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  photo_manager: ^3.0.0
  sqflite: ^2.3.0
  dio: ^5.4.0
  path_provider: ^2.1.0
  crypto: ^3.0.3
  image: ^4.1.0
  flutter_background_service: ^5.0.0
  permission_handler: ^11.0.0
  provider: ^6.1.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
